import React from 'react';
import {v1} from "uuid";
import {AchionsType, NavbarType} from "./State";

const NavbarReducer = (state: NavbarType[], action: AchionsType) => {
    return state;
};
export default NavbarReducer;